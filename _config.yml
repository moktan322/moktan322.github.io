title: Foo Bar
email: bloggerfoobar@yahoo.com
description: >- # this means to ignore newlines until "baseurl:"
  I am Foo Bar. This is my blogging platform. Programmers use me as a
  placeholder name in their Computer programs to name entities such as
  Variables, Functions, Commands etc to demonstrate more concept
  than the identity.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: bloggerfoobar

# Build settings
markdown: kramdown
theme: minima
plugins:
  - jekyll-feed
  - jekyll-seo-tag

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
